<Window x:Class="b1_chat_console.SceneBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Open Scene" Width="720" Height="540" MinWidth="560" MinHeight="420"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource PageBackgroundBrush}"
        Icon="Assets/AppIcon.ico"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="SceneRowBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderLoBrush}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="12,10" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentDimBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,14">
            <Button DockPanel.Dock="Right" Content="+ New Scene" Style="{StaticResource MiniButtonStyle}"
                    Click="NewScene_Click" Margin="12,0,0,0"
                    ToolTip="Close this browser and create a new empty Scene." />
            <StackPanel>
                <TextBlock Text="OPEN SCENE" Style="{StaticResource CardTitleStyle}" Margin="0" />
                <TextBlock Text="Choose a Scene from the Local Library. Double-click to open it."
                           Style="{StaticResource MetaTextStyle}" Margin="0,3,0,0" />
            </StackPanel>
        </DockPanel>

        <TextBox x:Name="SearchBox" Grid.Row="1" Style="{StaticResource RecessedTextBoxStyle}"
                 Margin="0,0,0,12" Padding="9,7" TextChanged="SearchBox_TextChanged"
                 ToolTip="Filter Scenes by name." />

        <Border Grid.Row="2" Background="{StaticResource RecessBrush}" BorderBrush="{StaticResource BorderLoBrush}"
                BorderThickness="1" CornerRadius="6">
            <Grid>
                <ListBox x:Name="SceneList" Background="Transparent" BorderThickness="0"
                         ItemsSource="{Binding Scenes}" SelectionChanged="SceneList_SelectionChanged"
                         MouseDoubleClick="SceneList_MouseDoubleClick"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource SceneRowBorderStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="170" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextStrongBrush}"
                                                       FontSize="13" FontWeight="SemiBold" />
                                            <Border Margin="9,0,0,0" Padding="6,1" CornerRadius="7"
                                                    Background="{StaticResource AccentDimBrush}"
                                                    Visibility="{Binding IsCurrent, Converter={StaticResource BoolToVisConv}}">
                                                <TextBlock Text="OPEN" Foreground="{StaticResource AccentBrush}"
                                                           FontSize="9" FontWeight="Bold" />
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding ContentsText}" Style="{StaticResource MetaTextStyle}" Margin="0,3,0,0" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding SavedText}" Foreground="{StaticResource TextBrush}" FontSize="11" HorizontalAlignment="Right" />
                                        <TextBlock Text="Last saved" Style="{StaticResource MetaTextStyle}" HorizontalAlignment="Right" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center"
                            IsHitTestVisible="False">
                    <TextBlock x:Name="EmptyTitle" Text="No Scenes found" Foreground="{StaticResource TextStrongBrush}"
                               FontSize="14" HorizontalAlignment="Center" />
                    <TextBlock x:Name="EmptyHint" Text="Create a new Scene to begin."
                               Style="{StaticResource MetaTextStyle}" Margin="0,4,0,0" HorizontalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding LibraryStatus}" Style="{StaticResource MetaTextStyle}"
                       VerticalAlignment="Center" ToolTip="{Binding LibraryIssueText}" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Cancel" Style="{StaticResource MiniButtonStyle}" IsCancel="True" MinWidth="82" Margin="0,0,8,0" />
                <Button x:Name="OpenButton" Content="Open" Style="{StaticResource AccentButtonStyle}"
                        IsDefault="True" IsEnabled="False" MinWidth="82" Click="OpenScene_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
